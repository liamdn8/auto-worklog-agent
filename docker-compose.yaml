services:
  aw-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aw-server
    ports:
      - "5600:5600"
    volumes:
      - aw_data:/data
    environment:
      - AW_DATA_DIR=/data
    restart: unless-stopped

volumes:
  aw_data:
